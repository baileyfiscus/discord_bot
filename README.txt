$echo
Repeats everything after the command
$suggest
Records suggestions for the bot so that Boolery can review and implement them
$randomGame
takes in parameter # of players and gives a suitable game suggestions
$listGames
takes in parameter # of players and gives a list of suitable games